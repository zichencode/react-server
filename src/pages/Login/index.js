import React from 'react';
import Header from '../../components/Header'
const Home = () => {
    return (
        <div>
            <div><Header /></div>
        login
            <button onClick={() => alert(1)} index="5">点击</button>
        </div>
    )
}

export default Home;