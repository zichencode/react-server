import React from 'react';
const Home = () => {
    return (
        <div>
        login
            <button onClick={() => alert(1)} index="5">点击</button>
        </div>
    )
}

export default Home;